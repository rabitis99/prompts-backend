# 코드 문제점 분석

## 📋 목차
- [코드 품질 이슈](#코드-품질-이슈)
- [배포 관련 문제점](#배포-관련-문제점)
  - [🔴 치명적 문제 (배포 전 반드시 해결 필요)](#-치명적-문제-배포-전-반드시-해결-필요)
  - [🟠 중요 문제 (배포 전 해결 권장)](#-중요-문제-배포-전-해결-권장)
  - [🟡 개선 권장 사항](#-개선-권장-사항)
  - [📋 배포 전 체크리스트](#-배포-전-체크리스트)

## 🔗 배포 문제와의 연관성

### 코드 문제 → 배포 문제 해결 방향

**코드 문제를 해결하면 배포 문제도 해결되는 경우:**
- ✅ **항목 10 (PromptPersistenceService)** → [`배포_시_문제점_분석.md`](./배포_시_문제점_분석.md) 항목 11과 동일 문제
  - 트랜잭션 전파 전략 문제를 코드에서 해결하면 배포 문제도 해결됨

**코드 문제를 해결하면 배포 문제도 부분적으로 해결되는 경우:**
- ⚠️ **항목 22 (NotificationServiceImpl)** → [`배포_시_문제점_분석.md`](./배포_시_문제점_분석.md) 항목 5와 관련
  - 코드 설계 개선 시 알림 시스템 확장성 향상되어 배포 문제 해결에 도움됨
  - 단, 실제 알림 발송 로직 구현은 별도 작업 필요

**코드 문제를 해결해도 배포 문제가 자동으로 해결되지 않는 경우:**
- 대부분의 배포 문제는 **인프라/설정/운영** 문제입니다
- 예: 환경 변수 설정, DB 마이그레이션 도구, ShedLock 테이블 생성, Redis/RabbitMQ 호스트 설정 등

### 배포 문제 → 코드 문제 해결 방향

**배포 문제를 해결하면 코드 문제도 해결되는 경우:**
- ✅ **배포 항목 11 (트랜잭션 전파 전략)** → 코드 항목 10과 동일 문제

**배포 문제를 해결해도 코드 문제가 자동으로 해결되지 않는 경우:**
- ⚠️ **배포 항목 5 (알림 시스템)** → 코드 항목 22와 관련
  - 알림 기능을 구현해도 설계 문제(책임 분리)는 별도로 해결 필요

> 💡 **배포 시 문제점은 별도 문서로 관리됩니다**  
> 상세한 배포 관련 문제점과 해결 상태는 [`배포_시_문제점_분석.md`](./배포_시_문제점_분석.md)를 참조하세요.

---

## 코드 품질 이슈

### 🔧 설정 및 Bean 관리

#### 1. RebuildStatusServiceConfig ✅ 해결됨
**파일**: `src/main/java/org/example/sharedprompts/domain/admin/maintenance/config/RebuildStatusServiceConfig.java`

**문제점**: 두 개의 Bean 메서드에 모두 `@Primary` 어노테이션이 적용되어 있음. 조건부로 하나만 활성화되지만, 같은 타입에 두 개의 `@Primary`는 Spring 컨테이너에서 모호성을 야기할 수 있음

**해결 완료**: ✅ `@Primary` 어노테이션 제거, `@ConditionalOnProperty`만 사용하여 조건부 활성화

**브랜치**: `refactor/admin-rebuild-status-primary-conflict`

---

#### 2. SchedulerConfig ✅ 해결됨
**파일**: `src/main/java/org/example/sharedprompts/global/config/scheduler/SchedulerConfig.java`

**문제점**: `@Primary` 어노테이션이 두 개의 Bean 메서드에 모두 적용됨. 조건부 활성화 구조에서 모호성 발생 가능

**해결 완료**: ✅ `@Primary` 어노테이션 제거, `@ConditionalOnProperty`만 사용하여 조건부 활성화

**브랜치**: `refactor/scheduler-primary-bean-conflict`

---

#### 3. 패키지명 오타 ✅ 해결됨
**파일**: `src/main/java/org/example/sharedprompts/global/config/scheduler/`

**문제점**: 디렉토리 이름에 오타가 있음 (`shcduler` → `scheduler`). 패키지 이름 오타는 리팩토링 시 모든 import 수정이 필요하여 유지보수성 저하

**해결 완료**: ✅ 패키지명을 `scheduler`로 수정 완료

**브랜치**: `chore/fix-scheduler-package-typo`

---

#### 4. AsyncExecutorConfig ✅ 해결됨
**파일**: `src/main/java/org/example/sharedprompts/global/config/async/AsyncExecutorConfig.java`

**문제점**: Executor 생성 메서드 간 유사 로직 반복

**해결 완료**: ✅ 공통 생성 로직 추출 완료
- `configureExecutor()`: 공통 설정 로직 추출
- `createExecutor()`: SecurityContext 없는 일반 Executor 생성
- `createExecutorWithSecurityContext()`: SecurityContext 포함 Executor 생성
- `createExecutorService()`: ExecutorService 생성
- 각 Bean 메서드가 공통 메서드를 호출하여 중복 제거

**브랜치**: `refactor/async-executor-duplication`

---

#### 5. AsyncConfig ✅ 해결됨
**파일**: `src/main/java/org/example/sharedprompts/global/config/async/config/AsyncConfig.java`

**문제점**: `taskExecutor()` Bean을 내부에서 다시 호출하여 Bean 중복 생성 위험

**해결 완료**: ✅ 생성자 주입을 통해 Bean을 주입받아 사용하도록 변경
- `private final Executor taskExecutor;` 생성자 주입으로 Bean 주입
- `getAsyncExecutor()` 메서드에서 주입받은 Bean 반환
- Bean 중복 생성 문제 해결

**브랜치**: `fix/async-executor-bean-duplication`

---

### 🛡️ 보안 및 인증

#### 6. JwtAuthenticationFilter ✅ 해결됨
**파일**: `src/main/java/org/example/sharedprompts/auth/jwt/filter/JwtAuthenticationFilter.java`

**문제점**: JWT 검증 로직이 필터에 과도하게 집중됨

**해결 완료**: ✅ JWT 검증 로직을 `JwtTokenValidationService`로 분리
- 필터는 서비스를 호출하여 검증 수행
- 검증 로직은 서비스에 위임하여 테스트 및 유지보수성 향상
- 필터는 인증 성공 시 SecurityContext 설정만 담당

**브랜치**: `refactor/jwt-authentication-filter-responsibility`

---

#### 7. PromptFacade ✅ 해결됨
**파일**: `src/main/java/org/example/sharedprompts/domain/prompt/facade/PromptFacade.java`

**문제점**: Facade에서 SecurityContext 직접 조작

**해결 완료**: ✅ `SecurityContextService`를 통해 SecurityContext 접근
- SecurityContext 직접 조작 대신 전용 서비스 사용
- 비즈니스 로직과 보안 책임 분리 완료

**브랜치**: `refactor/prompt-facade-security-context`

---

#### 8. AuthServiceImpl (✅ 리팩토링 완료)
**파일**: `src/main/java/org/example/sharedprompts/domain/auth/service/AuthServiceImpl.java`

**문제점**: 
- 과도한 의존성 주입 (12개) - 다른 서비스들(6개)의 2배
- 단일 책임 원칙(SRP) 위반 가능성
- 테스트 복잡도 증가

**해결 방안**: 
- `SignUpService`: 회원가입 관련 로직 통합 (PasswordEncoder, PasswordStrengthValidator, UserRegistrationService)
- `LoginService`: 로그인 관련 로직 통합 (PasswordVerifier, UserValidationService)
- `TokenSecurityService`: 토큰 보안 검증 통합 (TokenSecurityValidator, TokenSecurityResultHandler)
- 의존성 12개 → 8개로 감소 (33% 감소)

**브랜치**: `refactor/auth-service-dependency-reduction`

---

#### 9. SecurityUtils ✅ 해결됨
**파일**: `src/main/java/org/example/sharedprompts/global/util/SecurityUtils.java`

**문제점**: 타이밍 공격 취약점 주석만 있고 실제 안전한 비교 구현 없음

**해결 완료**: ✅ `constantTimeEquals()` 메서드 구현 완료 (MessageDigest.isEqual() 사용)

**브랜치**: `fix/security-utils-timing-attack`

---

### 🔄 트랜잭션 관리

#### 10. PromptPersistenceService ✅ 해결됨
**파일**: `src/main/java/org/example/sharedprompts/domain/prompt/service/PromptPersistenceService.java`

**문제점**: `REQUIRES_NEW` 전파 사용으로 외부 트랜잭션과 롤백 분리

**해결 방안**: 저장 실패 시 상위 트랜잭션 롤백되지 않을 수 있으므로 전파 전략 재검토 필요

**브랜치**: `refactor/prompt-transaction-propagation`

**해결 내용**: `REQUIRES_NEW` 제거하고 기본 전략(`REQUIRED`)으로 변경하여 상위 트랜잭션에 참여하도록 수정

> 🔗 **배포 연관**: [`배포_시_문제점_분석.md`](./배포_시_문제점_분석.md) 항목 11과 동일 문제. 코드 해결 시 배포 문제도 해결됨.

---

#### 11. PromptServiceImpl ✅ 해결됨
**파일**: `src/main/java/org/example/sharedprompts/domain/prompt/service/PromptServiceImpl.java`

**문제점**: 조회 메서드에 `@Transactional` 사용

**해결 방안**: 쓰기 로직과 분리 또는 `readOnly = true` 적용 필요

**브랜치**: `refactor/prompt-service-transaction-scope`

**해결 내용**: `getPromptDetail` 메서드를 `readOnly = true`로 변경하고, 쓰기 작업(`incrementUsageCount`)을 별도 메서드로 분리하여 `REQUIRES_NEW` 전파 전략 적용

---

### 🎯 타입 안전성

#### 12. GlobalExceptionHandler ✅ 해결됨
**파일**: `src/main/java/org/example/sharedprompts/global/exception/GlobalExceptionHandler.java`

**문제점**: 엔티티 타입을 문자열 비교로 판단. 클래스명 변경 시 동작하지 않으며 타입 안전성 없음

**해결 완료**: ✅ `OptimisticLockEntityType` enum을 사용하여 타입 안전성 확보
- 예외 타입을 enum 기반으로 관리
- 제약 조건 이름 기반 매핑으로 안정적인 예외 처리

**브랜치**: `refactor/global-exception-entity-type-check`

---

#### 13. TagCountMetricService ✅ 해결됨
**파일**: `src/main/java/org/example/sharedprompts/domain/tag/count/TagCountMetricService.java`

**문제점**: "increase", "decrease" 문자열 비교 분기

**해결 완료**: ✅ `TagCountOperation` enum을 사용하여 타입 안전성 확보
- `INCREASE`, `DECREASE` enum으로 문자열 비교 제거
- 각 enum이 메트릭 기록 로직을 담당하는 전략 패턴 적용

**브랜치**: `refactor/tag-count-operation-enum`

---

#### 14. LocalRebuildStatusManager ✅ 해결됨
**파일**: `src/main/java/org/example/sharedprompts/domain/admin/maintenance/rebuild/util/LocalRebuildStatusManager.java`

**문제점**: 예외 타입을 `getSimpleName()` 문자열로 사용

**해결 완료**: ✅ `RebuildFailureType` enum을 사용하여 타입 안전성 확보
- 예외 타입을 enum 기반으로 관리 (`RebuildFailureType.from(e)`)
- 예외 클래스 타입을 기반으로 매핑하여 타입 안전성 확보

**브랜치**: `refactor/rebuild-exception-type-management`

---

### 📝 코드 중복 및 구조

#### 15. AdminController ✅ 해결됨
**파일**: `src/main/java/org/example/sharedprompts/controller/admin/AdminController.java`

**문제점**: keyword trim 로직이 `getUsers()`와 `getPrompts()`에 중복

**해결 완료**: ✅ `StringUtils.trimToNull()` 유틸리티 메서드로 중복 제거
- 공통 문자열 처리 로직을 `StringUtils` 클래스로 분리
- `getUsers()`와 `getPrompts()`에서 공통 유틸 사용

**브랜치**: `refactor/admin-controller-keyword-trim`

---

### ⚡ 성능 최적화

#### 16. PromptLikeDomainService ✅ 최적화됨
**파일**: `src/main/java/org/example/sharedprompts/domain/like/service/PromptLikeDomainService.java`

**설명**: 
- `like()` 메서드: `findById()`를 사용하여 엔티티를 조회 (FK 검증 필요)
- `unlike()` 메서드: `existsById()`만 사용하여 존재 여부 확인 후 `deleteById()` 호출
- 불필요한 `existsById()` + `getReferenceById()` 패턴은 사용하지 않음

**결론**: 현재 구현은 적절함. `unlike()`에서 `existsById()`는 삭제 전 존재 여부 확인을 위해 필요함

**브랜치**: `refactor/prompt-like-existence-check`

---

### 🏗️ 설계 및 아키텍처

#### 17. PromptGenerator ✅ 의도적인 설계 결정
**파일**: `src/main/java/org/example/sharedprompts/domain/prompt/service/PromptGenerator.java`

**설명**: 프롬프트 템플릿이 영어로 하드코딩되어 있으나, 이는 **의도적인 설계 결정**입니다.

**이유**: LLM(대형 언어 모델)은 영어에서 가장 우수한 성능을 보이므로, 프롬프트 템플릿을 영어로 고정하여 최적의 결과를 얻기 위한 설계입니다.

**결론**: 해결 불필요 (의도적인 설계)

---

#### 18. StatisticsCacheEventListener ✅ 해결됨
**파일**: `src/main/java/org/example/sharedprompts/domain/statistics/event/StatisticsCacheEventListener.java`

**문제점**: 캐시 키가 하드코딩된 상수로 분산 관리됨

**해결 완료**: ✅ `StatisticsCacheKeys` 상수 클래스 생성하여 캐시 키 중앙화
- 통계 캐시 키를 `StatisticsCacheKeys` 클래스로 중앙 관리
- `userStatistics()` 메서드로 개인 통계 캐시 키 생성 로직 통합
- 캐시 키 변경 시 한 곳만 수정하면 되도록 개선

**브랜치**: `refactor/statistics-cache-key-centralization`

---

#### 19. AdminMaintenanceServiceImpl ✅ 해결됨
**파일**: `src/main/java/org/example/sharedprompts/domain/admin/maintenance/service/AdminMaintenanceServiceImpl.java`

**문제점**: `rebuildLikeCountsFromDbAsync()`가 상태 관리, 재시도, 알림, 메트릭까지 담당

**해결 완료**: ✅ 책임 분리 완료
- `RebuildNotificationService`: 알림 책임 분리
- `RetryableRebuildService`: 재시도 책임 분리
- `RebuildMetrics`: 메트릭 책임 분리
- `LocalRebuildStatusManager`: 로컬 상태 관리 책임 분리
- `RebuildStatusService`: 글로벌 상태 관리 책임 분리
- 서비스는 오케스트레이션 역할만 담당

**브랜치**: `refactor/admin-maintenance-service-responsibility`

---

#### 20. PromptController ✅ 해결됨
**파일**: `src/main/java/org/example/sharedprompts/controller/prompt/PromptController.java`

**해결 완료**: ✅ `PromptFacade`로 비동기 처리 책임 위임
- 컨트롤러는 `promptFacade.createPromptAsyncWeb()` 호출만 담당
- 비동기 처리 로직은 Facade 레이어로 분리

**브랜치**: `refactor/prompt-controller-async-delegation`

---

#### 21. AuditLogAspect ✅ 해결됨
**파일**: `src/main/java/org/example/sharedprompts/global/aspect/AuditLogAspect.java`

**문제점**: Aspect에서 파라미터 추출 로직이 과도하게 복잡

**해결 완료**: ✅ 파라미터 추출 로직을 전용 유틸리티 클래스로 분리
- `ParameterExtractor`: 파라미터 추출 책임 분리
- `ActorExtractor`: Actor 정보 추출 책임 분리
- `AuditActionResolver`: AuditAction 결정 책임 분리
- `DescriptionBuilder`: Description 생성 책임 분리
- Aspect는 오케스트레이션 역할만 담당

**브랜치**: `refactor/audit-log-aspect-parameter-extraction`

---

#### 22. NotificationServiceImpl
**파일**: `src/main/java/org/example/sharedprompts/domain/notification/service/core/NotificationServiceImpl.java`

**문제점**: 알림 생성·조회·읽음 처리 책임이 단일 서비스에 집중

**해결 방안**: 전략/팩토리 패턴으로 확장성 개선 필요

**브랜치**: `refactor/notification-service-strategy`

> 🔗 **배포 연관**: [`배포_시_문제점_분석.md`](./배포_시_문제점_분석.md) 항목 5와 관련. 코드 설계 개선 시 알림 시스템 확장성 향상되어 배포 문제 해결에 도움됨.

---

## 배포 관련 문제점

배포 시 발생할 수 있는 문제점들을 정리했습니다. 대부분의 배포 문제는 **인프라/설정/운영** 문제로 코드 문제와 별개입니다.

### ⚠️ 양방향 해결 관계 요약

**양방향으로 해결 가능한 문제:**
- ✅ **트랜잭션 전파 전략** (코드 항목 10 ↔ 배포 항목 11)
  - 코드에서 해결 → 배포 문제 해결
  - 배포 문제 해결 → 코드 문제 해결

**일방향으로만 해결 가능한 문제:**
- ⚠️ **알림 시스템** (코드 항목 22 ↔ 배포 항목 5)
  - 코드 설계 개선 → 배포 문제 부분 해결 (확장성 향상)
  - 배포 문제 해결(기능 구현) → 코드 설계 문제는 별도 해결 필요

**서로 무관한 문제:**
- 대부분의 배포 문제는 **인프라/설정/운영** 문제로 코드 문제와 무관
- 예: 환경 변수 설정, DB 마이그레이션 도구, ShedLock 테이블 생성, Redis/RabbitMQ 호스트 설정 등

> ⚠️ **중요**: 코드 문제만 해결해서는 배포 문제가 해결되지 않습니다!  
> 대부분의 배포 문제는 **인프라/설정/운영** 작업이 필요합니다.

---

## 🔴 치명적 문제 (배포 전 반드시 해결 필요)

### 23. 필수 환경 변수 누락으로 인한 애플리케이션 시작 실패 ✅ 해결됨
**위치**: `src/main/resources/application.yml`

**문제점**:
- 필수 환경 변수에 기본값이 없어 설정 누락 시 애플리케이션이 시작되지 않음
- 다음 환경 변수들이 필수이지만 기본값이 없음:
  - `JWT_SECRET`, `SPRING_DATASOURCE_URL`, `SPRING_DATASOURCE_USERNAME`, `SPRING_DATASOURCE_PASSWORD`
  - `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `NAVER_CLIENT_ID`, `NAVER_CLIENT_SECRET`, `KAKAO_CLIENT_ID`, `KAKAO_CLIENT_SECRET`
  - `CORS_ALLOWED_ORIGINS`, `OAUTH2_REDIRECT_FRONT_URL`, `OAUTH2_FAILURE_REDIRECT_URL`, `OAUTH2_SALT`, `GOOGLE_GEMINI_API_KEY`

**영향**: 애플리케이션이 시작되지 않아 서비스 중단

**해결 완료**:
- ✅ `EnvironmentValidator` 클래스 추가: 애플리케이션 시작 시 필수 환경 변수 자동 검증
- ✅ 누락된 환경 변수가 있으면 애플리케이션 시작 실패 (IllegalStateException)

**브랜치**: `infra/environment-validation`

---

### 24. 더미 API 키로 인한 외부 서비스 호출 실패 ✅ 해결됨
**위치**: `src/main/resources/application.yml:129`

**문제점**: OpenAI API 키가 더미 값으로 기본 설정되어 있음

**영향**: AI 기능이 동작하지 않음

**해결 완료**:
- ✅ `EnvironmentValidator`에서 프로덕션 환경에서 더미 API 키 사용 시 애플리케이션 시작 실패

**브랜치**: `infra/dummy-api-key-validation`

---

### 25. 프로덕션 프로필 미활성화로 인한 개발 설정 적용 ✅ 해결됨
**위치**: `src/main/resources/application-prod.yml`

**문제점**: 프로덕션 프로필이 명시적으로 활성화되지 않으면 개발 환경 설정이 적용될 수 있음

**영향**: 개발용 설정이 프로덕션에 적용됨

**해결 완료**:
- ✅ `EnvironmentValidator`에서 프로덕션 환경 감지 시 `prod` 프로필 필수 검증

**브랜치**: `infra/prod-profile-validation`

---

### 26. ShedLock 테이블 미생성으로 인한 스케줄러 비동작
**위치**: `src/main/resources/application.yml:156`

**문제점**: ShedLock 테이블 자동 생성이 `false`로 설정되어 있음. 프로덕션 환경에서 테이블이 없으면 스케줄러가 동작하지 않음

**영향**: 배치 작업이 실행되지 않음

**해결 방안**: 배포 전 `shedlock` 테이블 생성 확인 또는 마이그레이션 스크립트 실행

**브랜치**: `ops/shedlock-table-verification`

---

### 27. 알림 시스템 미구현으로 인한 장애 인지 불가 ⏳ 배포 후 추가 예정
**위치**: 
- `src/main/java/org/example/sharedprompts/domain/admin/maintenance/rebuild/RebuildNotificationService.java`
- `src/main/java/org/example/sharedprompts/global/config/async/AsyncExceptionNotifierImpl.java`

**문제점**: 알림 발송 로직이 TODO로 남아있어 실제 알림이 발송되지 않음

**영향**: 운영 중 문제 발생 시 즉시 인지 불가

**계획**: ⏳ 배포 후 Slack, Email, PagerDuty 등 실제 알림 시스템 연동 예정

**브랜치**: `feature/notification-integration`

> 🔗 **코드 연관**: 코드 항목 22와 관련. NotificationServiceImpl의 설계 개선 시 알림 시스템 확장성 향상됨.

---

## 🟠 중요 문제 (배포 전 해결 권장)

### 28. Actuator 엔드포인트 보안 설정 부재 ✅ 해결됨
**위치**: `src/main/resources/application.yml:337-348`

**문제점**: Actuator 엔드포인트가 인증 없이 노출됨

**영향**: 시스템 정보 유출, 보안 취약점

**해결 완료**:
- ✅ `/actuator/health`는 공개 접근 허용
- ✅ `/actuator/info`, `/actuator/metrics`, `/actuator/prometheus`는 관리자(ROLE_ADMIN) 전용

**브랜치**: `security/actuator-endpoint-protection`

---

### 29. 데이터베이스 마이그레이션 도구 미설정
**위치**: `build.gradle`, `src/main/resources/application.yml`

**문제점**: Flyway나 Liquibase 같은 마이그레이션 도구가 설정되어 있지 않음

**영향**: 배포 시 스키마 변경 누락 가능, 롤백 시 스키마 복구 어려움

**해결 방안**: Flyway 또는 Liquibase 도입

**브랜치**: `infra/db-migration-tool`

---

### 30. RabbitMQ 기본 자격 증명 사용
**위치**: `src/main/resources/application.yml:110-111`

**문제점**: 기본값이 `guest/guest`로 설정되어 있음

**영향**: 보안 취약점

**해결 방안**: 프로덕션 환경에서는 반드시 강력한 자격 증명으로 변경

**브랜치**: `security/rabbitmq-credentials`

---

### 31. Redis/RabbitMQ 호스트 기본값이 localhost
**위치**: `src/main/resources/application.yml:97, 108`

**문제점**: 기본값이 `localhost`로 설정되어 있음

**영향**: 외부 서비스 연결 실패, 애플리케이션 시작 실패 가능

**해결 방안**: 프로덕션 환경에서는 반드시 실제 호스트 주소로 설정

**브랜치**: `infra/external-service-config`

---

### 32. Graceful Shutdown 설정 부재 ✅ 해결됨
**위치**: `src/main/resources/application.yml`

**문제점**: Graceful shutdown 설정이 없음

**영향**: 데이터 무결성 문제, 사용자 요청 실패

**해결 완료**: ✅ `application.yml`에 Graceful Shutdown 설정 추가

**브랜치**: `infra/graceful-shutdown`

---

### 33. 트랜잭션 전파 전략 문제 ✅ 해결됨
**위치**: `src/main/java/org/example/sharedprompts/domain/prompt/service/PromptPersistenceService.java:35`

**문제점**: `REQUIRES_NEW` 사용으로 외부 트랜잭션과 독립적으로 동작

**영향**: 데이터 일관성 문제

**해결 완료**: ✅ `REQUIRES_NEW` 제거하고 기본 전략(`REQUIRED`)으로 변경하여 상위 트랜잭션에 참여하도록 수정
- JavaDoc 주석으로 트랜잭션 전파 전략 명시
- 프롬프트 저장 실패 시 상위 트랜잭션도 함께 롤백되어 데이터 일관성 보장

**브랜치**: `refactor/prompt-transaction-propagation`

> 🔗 **코드 연관**: 코드 항목 10과 동일 문제. 코드에서 해결하여 배포 문제도 해결됨.

---

### 34. 하드코딩된 타임아웃 값 ✅ 해결됨
**위치**: `src/main/java/org/example/sharedprompts/domain/prompt/facade/PromptFacade.java`

**문제점**: 타임아웃 값이 하드코딩되어 있음 (`TIMEOUT_MS = 40_000L`)

**영향**: 환경별 성능 차이

**해결 완료**: ✅ 타임아웃 값을 설정 파일로 외부화

**브랜치**: `refactor/prompt-timeout-externalization`

---

## 🟡 개선 권장 사항

### 35. CORS 설정 검증 필요 ⏳ 추후 처리 예정
**위치**: `src/main/resources/application.yml:204`

**문제점**: CORS 설정이 환경 변수로만 관리되어 설정 누락 시 CORS 오류 발생 가능

**계획**: ⏳ 앱 개발 완료 후 CORS 설정 검증 로직 추가 예정

**브랜치**: `refactor/cors-app-web-separation`

---

### 36. OAuth2 리다이렉트 URL 검증 필요 ✅ 부분 해결됨
**위치**: `src/main/resources/application.yml:178`

**문제점**: OAuth2 리다이렉트 URL이 환경 변수로 관리되는데, 실제 OAuth 제공자에 등록된 URL과 일치해야 함

**해결 완료**: ✅ `EnvironmentValidator`에서 OAuth2 리다이렉트 URL 형식 검증 추가

**브랜치**: `security/oauth2-url-validation`

---

### 37. 커넥션 풀 크기 최적화 필요 ✅ 경고 추가됨
**위치**: `src/main/resources/application-prod.yml:22-23`

**문제점**: 실제 트래픽에 맞게 조정하지 않으면 커넥션 부족 또는 리소스 낭비 발생

**해결 완료**: ✅ `EnvironmentValidator`에서 프로덕션 환경에서 기본값 사용 시 경고 로그 출력

**브랜치**: `infra/connection-pool-optimization`

---

### 38. 로깅 설정 최적화 필요 ✅ 검증 추가됨
**위치**: `src/main/resources/application.yml:387-390`

**문제점**: 프로덕션 환경에서 적절한 로깅 레벨 설정 필요

**해결 완료**: ✅ `EnvironmentValidator`에서 프로덕션 환경에서 DEBUG/TRACE 레벨 사용 시 경고 로그 출력

**브랜치**: `infra/logging-optimization`

---

### 39. JPA DDL Auto 설정 검증 필요 ✅ 해결됨
**위치**: `src/main/resources/application.yml:85`

**문제점**: 프로덕션 환경에서 실수로 `update`가 설정되면 데이터 손실 위험

**해결 완료**: ✅ `EnvironmentValidator`에서 프로덕션 환경에서 위험한 DDL 설정 사용 시 애플리케이션 시작 실패

**브랜치**: `infra/ddl-auto-validation`

---

### 40. Health Check 엔드포인트 설정 ✅ 해결됨
**위치**: `src/main/resources/application.yml:343-344`

**문제점**: Health check 상세 정보가 인증 시에만 표시되지만, 인증 설정이 없음

**해결 완료**: ✅ Actuator 엔드포인트 보안 설정 완료 (항목 28 참조)

**브랜치**: `security/health-check-endpoint`

---

## 📋 배포 전 체크리스트

### 필수 환경 변수 설정
- [ ] `JWT_SECRET`
- [ ] `SPRING_DATASOURCE_URL`, `SPRING_DATASOURCE_USERNAME`, `SPRING_DATASOURCE_PASSWORD`
- [ ] `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`
- [ ] `NAVER_CLIENT_ID`, `NAVER_CLIENT_SECRET`
- [ ] `KAKAO_CLIENT_ID`, `KAKAO_CLIENT_SECRET`
- [ ] `CORS_ALLOWED_ORIGINS`
- [ ] `OAUTH2_REDIRECT_FRONT_URL`, `OAUTH2_FAILURE_REDIRECT_URL`, `OAUTH2_SALT`
- [ ] `GOOGLE_GEMINI_API_KEY`, `SPRING_AI_OPENAI_API_KEY`

### 인프라 설정
- [ ] `SPRING_PROFILES_ACTIVE=prod` 설정
- [ ] `REDIS_HOST` (프로덕션 Redis 호스트)
- [ ] `RABBITMQ_HOST` (프로덕션 RabbitMQ 호스트)
- [ ] `RABBITMQ_USERNAME`, `RABBITMQ_PASSWORD` (강력한 자격 증명)
- [ ] ShedLock 테이블 생성 확인

### 보안 설정
- [x] Actuator 엔드포인트 인증 설정 ✅
- [x] Health check 엔드포인트 보안 설정 ✅
- [ ] CORS 설정 검증
- [x] OAuth2 리다이렉트 URL 검증 ✅

### 모니터링 및 알림
- [ ] 알림 시스템 연동 (Slack, Email 등)
- [ ] 비동기 예외 알람 시스템 연동
- [ ] 로그 수집 시스템 연동
- [ ] 메트릭 수집 시스템 연동 (Prometheus)

### 성능 최적화
- [ ] 커넥션 풀 크기 조정 (부하 테스트 기반) ⚠️ 경고 추가됨
- [x] Graceful shutdown 설정 ✅
- [x] 로깅 레벨 최적화 ✅ (검증 추가됨)

### 데이터베이스
- [ ] 마이그레이션 도구 설정 (Flyway/Liquibase)
- [x] JPA `ddl-auto=none` 확인 ✅ (검증 추가됨)
- [ ] 데이터베이스 백업 계획 수립

---

## 📊 통계

### 코드 품질 이슈
- **총 코드 이슈**: 21건 (항목 17은 의도적인 설계 결정으로 제외)
- **해결 완료**: 17건 (항목 1, 2, 3, 4, 5, 6, 7, 8, 9, 12, 13, 14, 15, 18, 19, 20, 21)
- **설계 문제**: 11건 (8건 해결됨: 항목 6, 7, 8, 12, 18, 19, 20, 21)
- **중복 코드**: 1건 (항목 15 해결됨)
- **비효율성**: 1건 (항목 16 - 최적화됨)
- **보안 이슈**: 1건 (해결됨)
- **타입 안전성**: 3건 (모두 해결됨: 항목 12, 13, 14)
- **의도적인 설계**: 1건 (항목 17 - 해결 불필요)

### 배포 관련 문제점
- **총 배포 이슈**: 18건
- **치명적 문제**: 5건 (3건 해결됨, 1건 대기, 1건 미해결)
- **중요 문제**: 7건 (5건 해결됨, 2건 미해결)
- **개선 권장**: 6건 (5건 해결됨, 1건 대기)

### 전체 통계
- **총 문제점**: 39건 (항목 17은 의도적인 설계 결정으로 제외)
- **해결 완료**: 30건 (코드 17건 + 배포 13건)
- **대기 중**: 2건
- **미해결**: 7건 (코드 1건: 항목 22 + 배포 6건)
- **의도적인 설계**: 1건 (항목 17 - 해결 불필요)

> 💡 **참고**: 이 문서는 코드 문제와 배포 문제를 모두 포함합니다.  
> 상세한 배포 관련 내용은 [`배포_시_문제점_분석.md`](./배포_시_문제점_분석.md)를 참조할 수 있습니다.